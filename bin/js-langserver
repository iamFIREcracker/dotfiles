#!/usr/bin/env bash

set -e

debug=0
for i; do
  if [ "$i" == "--debug" ]; then
    debug=1
  fi
done

if [ $debug -eq 0 ]; then
  node ~/opt/js-langserver/index.js "$@"
else
  node ~/opt/js-langserver/index.js "$@" 2>> ~/js-langserver.log
fi

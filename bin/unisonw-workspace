#!/usr/bin/env bash

set -e

exec >>~/unison-workspace.log 2>&1 # shut up, `watch`: https://stackoverflow.com/q/62536396
date

unisonw \
    "$HOME/Workspace" \
    "$HOME/Dropbox/unison/Workspace" \
    "$@"

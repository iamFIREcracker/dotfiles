#!/usr/bin/env bash

rlwrap \
  --multi-line=@ \
  --multi-line-ext .lisp \
  --only-cook '^\*' \
  --prompt-colour=blue \
  --histsize 1000 \
  --history-filename "$HOME/.rlwrap-lisp-history" \
  -- sbcl --dynamic-space-size 4000 --load ~/.sbclrc "$@"

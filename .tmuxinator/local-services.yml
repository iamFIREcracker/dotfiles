name: local-services
root: ~/

windows:
  - b1:
      root: ~/opt/bunny1
      panes:
        - vpython b1_custom.py --host localhost
  - cb: cb --listen
  - br: br --listen
  - sync-theme: alacritty-sync-theme
  # - ollama: echo sh -c "while : ; do ssh -v ollama.beast.tailscale.internal -N; sleep 3; echo -ne '\a'; done"
  # - proxy: ssh -t dabox.tailscale.internal bash -l "cb"; ssh -vN dabox.tailscale.internal-sock5

# ~/.tmuxinator/cnenv.yml

name: cnenv
root: ~/workspace/ConnectION

windows:
  - dabox:
      root: ~/workspace/ConnectION/
      panes:
        - vagrant status
  - vagrant:
      root: ~/workspace/ConnectION/connection/system/vagrant/
      panes:
        - vagrant status
  - neo4j:
      root: ~/workspace/ConnectION/neo4j-dev/
      panes:
        - g st

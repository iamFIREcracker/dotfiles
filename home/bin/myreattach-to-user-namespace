#!/usr/bin/env bash

reattach=`which reattach-to-user-namespace 2> /dev/null`

if [ -n "${reattach}" ]; then
    ${reattach} "$@"
else
    if [ "-l" == "$1" ]; then
        shift # skip
    fi
    "$@"
fi

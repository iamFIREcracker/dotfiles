#!/usr/bin/env bash

function paste() {
    if [ -e /dev/clipboard ]; then
        cat /dev/clipboard
    else
        pbpaste
    fi
}

function copy() {
    if [ -e /dev/clipboard ]; then
        cat >/dev/clipboard
    else
        pbcopy
    fi
}

test -t 0 && paste || copy

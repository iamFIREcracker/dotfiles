#!/usr/bin/env bash

function paste() {
    if [ -f /dev/clipboard ]; then
        cat /dev/clipboard
    else
        pbpaste
    fi
}

function copy() {
    if [ -f /dev/clipboard ]; then
        cat >/dev/clipboard
    else
        pbcopy
    fi
}

test -t 0 && paste || copy
